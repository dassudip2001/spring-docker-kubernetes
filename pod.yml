apiVersion: v1
kind: Pod
metadata:
  name: mysql-spring
  labels:
    name: mysql-spring
spec:
  containers:
  - name: mysql-spring
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 9090
